#include <stdio.h>
#include <math.h>
int main()
{
  int f=0,n, coeff[10],x,i;
  scanf("%d",&n);
  for(i=1;i<n;i++)
  scanf("%d",&coeff[i]);
  scanf("%d",&x); 
  for(i=n-1;i>=0;i--)
 {
   f=f * x + coeff[i];
   printf("%d",f);
 }
 return 0;
}
